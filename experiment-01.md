# Experiment 01: MessageChannel

In this experiment we create an application that can launch new browser windows and communicate with them using the `MessageChannel` API. The main window will send messages to the new window, and the new window will respond back.
